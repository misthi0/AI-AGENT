"""
Optional: CLI entry point for running agents outside of Streamlit.
For now, use: streamlit run app.py
"""

if __name__ == "__main__":
    print("Use 'streamlit run app.py' to start the web interface")
